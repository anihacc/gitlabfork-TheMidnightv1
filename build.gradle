/*
 * GradleWorks for ForgeGradle 5
 * Written by Jonathing
 * Version 5.0.0-alpha.12
 *
 * What was once just an effort to make life easier is now a fully-fledged automated plugin that does all of the work
 * for you! GradleWorks can be completely configured through the provided YAML files. If you're ever confused on what to
 * do, stop by the GradleWorks wiki on GitLab or the Jonathing Projects YouTube channel!
 */

// Buildscript setup (DO NOT EDIT!!!)
buildscript {
    repositories {
        // Modding Legacy Maven
        maven { url 'https://maven.moddinglegacy.com/maven' }

        // Minecraft Forge Maven
        maven { url 'https://maven.minecraftforge.net' }

        // SpongePowered Maven
        maven { url 'https://repo.spongepowered.org/maven' }

        // ParchmentMC Maven
        maven { url 'https://maven.parchmentmc.org' }
    }
    dependencies {
        // GradleWorks
        classpath group: 'me.jonathing', name: 'gradleworks', version: '5.0.0-alpha.12'

        // Minecraft Forge ForgeGradle
        classpath group: 'net.minecraftforge.gradle', name: 'ForgeGradle', version: '5.1.+', changing: true

        // SpongePowered MixinGradle
        classpath group: 'org.spongepowered', name: 'mixingradle', version: '0.7.+', changing: true

        // ParchmentMC Librarian
        classpath group: 'org.parchmentmc', name: 'librarian', version: '1.+', changing: true
    }
}

apply plugin: 'me.jonathing.gradleworks'
